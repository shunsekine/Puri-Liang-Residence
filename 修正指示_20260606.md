# 修正指示書 — Puri Liang Residence

- **対象ブランチ**: `feat/v2-bohemian-natural`
- **実装担当**: Antigravity
- **作成**: 2026-06-06 / Claude
- **原則**: 各修正後に必ず `npm run lint && npm run build` を通すこと。価格の「基準通貨は IDR（実支払い）」で、JPY/USD は参考換算表示。

---

## 確定パラメータ

### 価格換算表（IDRベース / JPY=÷110 / USD=÷18,000 / 参考価格として丸め）

| 部屋 | priceIDR（基準・実支払い） | priceJPY（参考） | priceUSD（参考） |
|---|---|---|---|
| Villa | 8,500,000 | 77,000 | 470 |
| King Studio | 6,500,000 | 59,000 | 360 |
| Twin Studio | 5,500,000 | 50,000 | 310 |
| 電気代 / 月 | 500,000 | 4,500 | 30 |

簡易レート: `100 JPY = 11,000 IDR`（=1JPY 110IDR） / `1 USD = 18,000 IDR`
ロケール→通貨: `ja→JPY` / `en→USD` / `id→IDR`

---

## 修正① 価格を IDR ベース化（ja→JPY / en→USD / id→IDR 表示）

### 1-A. `lib/data.ts`

**(1) `ROOMS` の価格を更新**（70〜107行付近）

| id | priceIDR | priceJPY | priceUSD |
|---|---|---|---|
| villa | `8500000` | `77000` | `470` |
| king | `6500000` | `59000` | `360` |
| twin | `5500000` | `50000` | `310` |

**(2) `SIMULATOR_DEFAULTS` を更新**（126行付近）

```ts
export const SIMULATOR_DEFAULTS = {
  electricityIDR: 500000,
  electricityJPY: 4500,   // 旧 electricityJPYPerMonth(9000) を改名・更新
  electricityUSD: 30,     // 追加
  discounts: [
    { months: 6, rate: 0.15 },
    { months: 3, rate: 0.10 },
  ],
} as const;
```

**(3) ファイル末尾に通貨ヘルパーを追加**

```ts
// 通貨表示（基準は IDR。表示のみ locale で切替。支払いは常に IDR）
export type CurrencyCode = 'JPY' | 'USD' | 'IDR';

const LOCALE_CURRENCY: Record<string, CurrencyCode> = { ja: 'JPY', en: 'USD', id: 'IDR' };

export function currencyForLocale(locale: string): CurrencyCode {
  return LOCALE_CURRENCY[locale] ?? 'IDR';
}

const SYMBOL: Record<CurrencyCode, string> = { JPY: '¥', USD: '$', IDR: 'Rp ' };

export function formatPrice(code: CurrencyCode, amount: number): string {
  return `${SYMBOL[code]}${amount.toLocaleString('en-US')}`;
}

export function roomPriceAmount(room: Room, code: CurrencyCode): number {
  return code === 'JPY' ? room.priceJPY : code === 'USD' ? room.priceUSD : room.priceIDR;
}

export function electricityAmount(code: CurrencyCode): number {
  return code === 'JPY' ? SIMULATOR_DEFAULTS.electricityJPY
       : code === 'USD' ? SIMULATOR_DEFAULTS.electricityUSD
       : SIMULATOR_DEFAULTS.electricityIDR;
}
```

### 1-B. 各表示箇所を locale 通貨に差し替え

> クライアント部品は `import { useLocale } from 'next-intl'` で locale 取得。サーバ部品も同 `useLocale()` 使用可。

**`components/pages/v2/RoomPreviewCard.tsx`**（47行）
```tsx
const locale = useLocale();
const code = currencyForLocale(locale);
// 47行:
<span className="p">{tCommon('approx')} {formatPrice(code, roomPriceAmount(room, code))}</span>
```
import に `useLocale`、`@/lib/data` から `currencyForLocale, formatPrice, roomPriceAmount` 追加。

**`app/[locale]/rooms/page.tsx`** RoomDetailRow（115行）
```tsx
const locale = useLocale();
const code = currencyForLocale(locale);
// 115行:
<span className="p">{tCommon('approx')} {formatPrice(code, roomPriceAmount(room, code))}</span>
```

**`app/[locale]/page.tsx`** ホーム Hero「from」価格（46行、現在 `¥48,000` 直書き）
```tsx
const locale = useLocale();
const code = currencyForLocale(locale);
const fromAmount = roomPriceAmount(ROOMS[ROOMS.length - 1], code); // 最安 = twin
// 46行:
<span className="p">{tCommon('approx')} {formatPrice(code, fromAmount)}</span>
```

**`components/pages/ReserveForm.tsx`**（計算ロジック含む・最重要）
- 先頭: `const locale = useLocale(); const code = currencyForLocale(locale);`
- `formatJPY()`（36行）廃止 → `formatPrice(code, n)` に置換
- 価格計算（91〜97行）を locale 通貨ベースに:
```tsx
const r = ROOMS.find(x => x.id === room)!;
const unitPrice = roomPriceAmount(r, code);
const rent = unitPrice * months;
const elec = electricityAmount(code) * months;
const discount = getDiscount(months);
const disc = Math.round(rent * discount);
const total = rent - disc + elec;
```
- 部屋選択ボタン価格（276行）→ `{tCommon('approx')} {formatPrice(code, roomPriceAmount(rr, code))}<span>/{tCommon('monthsUnit').slice(0,1)}</span>`
- サマリー各行（463〜492行）の `formatJPY(...)` / `¥{r.priceJPY...}` をすべて `formatPrice(code, ...)` / `unitPrice` ベースへ
- デポジット（491行）: `{formatPrice(code, unitPrice)}` ＋ 固定 `+ Rp 2,000,000`（IDR固定のまま）
- 成功画面合計（208行）: `formatPrice(code, total)`
- **Web3Forms payload**（135〜158行）: 請求は常に IDR のため、`currency: code` と IDR 建て金額（`total_idr` 等）も含める。`rent_jpy` 等のキー名は `rent`/`total` 等に改名し齟齬防止。

### 1-C. 「参考価格・支払いは IDR」明記（messages 3言語）

各 `messages/{ja,en,id}.json` の `Common` に追加:

| key | ja | en | id |
|---|---|---|---|
| `priceRefNote` | 参考価格・支払いは IDR | Reference price · billed in IDR | Harga referensi · dibayar dalam IDR |

`Reserve.summary.currencyNote` を更新:

| | ja | en | id |
|---|---|---|---|
| currencyNote | IDR 基準 · JPY 参考換算 | Billed in IDR · USD reference | Ditagih dalam IDR |

表示追加: rooms 詳細 `priceNote`（116行付近）下・ホーム Hero 価格付近・予約サマリーに `tCommon('priceRefNote')` を小さく表示。

---

## 修正② Twin Studio 画像が表示されない不具合

**原因**: ファイル名の半角スペースが CSS `url()` を破壊（Villa はスペース無しのため表示される）。

**(1) 画像ファイルをリネーム**
```
public/images/Room_Twin Studio_1.webp  →  public/images/Room_Twin_Studio_1.webp
public/images/Room_Twin Studio_2.webp  →  public/images/Room_Twin_Studio_2.webp
```

**(2) `lib/data.ts` の `IMG` 更新**（41〜42行）
```ts
twin1: '/images/Room_Twin_Studio_1.webp',
twin2: '/images/Room_Twin_Studio_2.webp',
```

**(3) 防御的措置**: 全描画箇所で `url()` をクォート化 `url("...")`
- `RoomPreviewCard.tsx` 21行 / `rooms/page.tsx` 78〜80行（3箇所）/ `ReserveForm.tsx` 438行（Villa・hero も同様推奨）

→ 修正後 Twin は写真2枚＋プレースホルダ1枠で表示（仕様通り）。

---

## 修正③ ホームの「3 rooms · Availability updated · Tukad Balian, Bali」を全言語削除

**`app/[locale]/page.tsx`** Hero badge ブロック削除（32〜35行）
```tsx
// 削除:
<div className="badge">
  <span className="dot" />
  {t('hero.badge')}
</div>
```
→ 単一部品なので全言語一括で消える。
（任意）`messages/{ja,en,id}.json` の `Home.hero.badge` キー（50行付近）も削除。

---

## 修正④ 予約画面: 両チェック時のみ送信ボタン有効化

**`components/pages/ReserveForm.tsx`**

**(1) チェック初期値を false に**（69〜70行）
```tsx
const [agreeRules, setAgreeRules] = useState(false);
const [agreeCancel, setAgreeCancel] = useState(false);
```

**(2) 送信可否フラグ追加**（handleSubmit 直前）
```tsx
const canSubmit = agreeRules && agreeCancel && status !== 'sending';
```

**(3) 送信ボタン更新**（412〜419行）
```tsx
<button
  type="submit"
  className={`v2-btn${canSubmit ? '' : ' is-disabled'}`}
  disabled={!canSubmit}
  style={{ padding: '16px 28px', fontSize: 14, width: '100%' }}
>
  {status === 'sending' ? t('submit.sending') : t('submit.idle')}
</button>
```

**(4) 無効時スタイル CSS 追加**（`app/globals.v2.pages.css` 末尾）
```css
.v2-btn.is-disabled,
.v2-btn:disabled {
  background: var(--v2-sand) !important;
  color: var(--v2-muted) !important;
  box-shadow: none !important;
  cursor: not-allowed;
  opacity: 0.6;
  pointer-events: none;
}
```
→ 両方チェックで初めて緑（通常 `.v2-btn`）になりクリック可。

---

## 修正⑤ Direct Contact 情報を全ページから削除（→予約フォーム導線に置換）

> Email/WhatsApp/Instagram 等の直接連絡先は全廃。**物件住所は現状維持**。

**(1) `ReserveForm.tsx` サマリー「直接問い合わせ」ブロック**（496〜501行）→ ブロックごと削除
**(2) `ReserveForm.tsx` 成功画面「急ぎのご連絡」ブロック**（216〜220行）→ ブロックごと削除
**(3) `components/common/Footer.tsx` Contact列**（49〜56行）→ 予約導線に置換
```tsx
<div>
  <h4>{t('contactHeading')}</h4>
  <ul>
    <li><Link href="/reserve">{tNav('reserve')}</Link></li>
    <li><Link href="/faq">{tNav('faq')}</Link></li>
  </ul>
</div>
```
**(4) `app/[locale]/page.tsx` 最下部CTAセクション セカンダリ（mailto, 138行）→ FAQ導線に置換（案B確定）**
```tsx
<Link href="/faq" className="v2-btn ghost">{t('cta.ctaSecondary')}</Link>
```
あわせて `Home.cta.ctaSecondary` のラベルを全言語で変更:
| | ja | en | id |
|---|---|---|---|
| ctaSecondary | よくある質問 | View FAQ | Lihat FAQ |
（プライマリ「予約フォーム →」は `/reserve` のまま。重複回避のためセカンダリは FAQ へ。）
**(5) messages 連絡先言及の修正**（3言語）: `Reserve.errors.networkError` の「WhatsApp / メールで直接お問い合わせください」→「恐れ入りますが、時間をおいて再度お試しください」相当へ。
**(6) 任意クリーンアップ**: 未使用化する `BRAND.email`/`BRAND.whatsapp`、`Reserve.summary.{contactTitle,whatsappLabel,emailLabel,responseLabel,responseValue}`、`Reserve.success.urgentTitle`、`ReserveForm.tsx` の `BRAND`/`tBrand` import を除去（lint 警告回避）。

---

## 修正⑥ Twin Studio 説明文の「同じ広さ」表現を全言語で削除

**理由**: 部屋タイプにより広さが異なるため「同じ広さ」は誤り。

`messages/{ja,en,id}.json` の `RoomData.twin.description`（各384行）を以下へ:

| 言語 | 修正後 |
|---|---|
| ja | `2名でシェアする友人・出張に。コストを抑えたい人向け。` |
| en | `Two friends or colleagues sharing a stay. Lower cost per person.` |
| id | `Untuk dua teman atau rekan kerja yang berbagi tempat. Hemat biaya per orang.` |

---

## 修正⑦ ホーム Hero の「空室を見る」ボタンを削除（全言語）

**理由**: リアルタイムの空室情報を掲載する予定がないため、「空室を見る」表現は不適切。

**`app/[locale]/page.tsx`** Hero の CTA 行（49〜52行）から **プライマリ（ctaPrimary）を削除**:
```tsx
// 削除（50行）:
<Link href="/rooms" className="v2-btn">{t('hero.ctaPrimary')}</Link>
```
→ セカンダリ「部屋について」（51行, ctaSecondary, →/rooms）のみ残す。
（任意）残ったボタンを主役にするため `className="v2-btn outline"` → `className="v2-btn"` に格上げしてもよい。
（任意）`messages/{ja,en,id}.json` の `Home.hero.ctaPrimary` キー（56行付近）を削除。

※ 単一部品のため全言語一括で反映される。

---

## 完了後チェックリスト

1. `npm run lint && npm run build` 成功
2. `/ja /en /id` × Home/Rooms/Reserve で価格が各通貨表示＋「参考価格・支払い IDR」注記
3. Twin Studio の写真が表示される
4. ホームのバナー文言が消えている
5. 予約: 2チェック未完で灰色・無効 → 両チェックで緑・有効
6. 連絡先（Email/WhatsApp）がどのページにも出ない／最下部CTAセカンダリが FAQ へ
7. Twin 説明文から「同じ広さ／same comfort／Luas yang sama」が消えている
8. 電気代が IDR 500,000（JPY 4,500 / USD 30）で計算されている
9. Hero から「空室を見る」ボタンが消え、「部屋について」のみ残っている（全言語）
